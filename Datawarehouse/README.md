## The Web Server that serves features via browser

Each folder contains a separate service and should run on a dedicated server

See each directory for how to setup the web server to work with the service
